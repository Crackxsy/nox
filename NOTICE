Nox
Copyright 2026 Nox contributors

This product includes software developed by the Nox contributors.
Nox's own source code is licensed under the Apache License, Version 2.0 (see LICENSE).

Third-party components are listed with their licenses in docs/THIRD_PARTY_LICENSES.md
(generated by scripts/license_audit.py; do not hand-edit).

GPL notice for the optional `voice` extra
-----------------------------------------
The optional `voice` extra depends on `piper-tts`, which pulls in `piper-phonemize` and
espeak-ng under the GNU General Public License, version 3 or later (GPL-3.0-or-later).

Apache-2.0 is one-way compatible with GPL-3.0: Apache-2.0 code may be combined into a
GPL-3.0 work, but not the reverse. Therefore:

  * Nox's source distribution and any installation without the `voice` extra are
    Apache-2.0 only.
  * Any distribution that bundles the `voice` extra - in particular the Windows
    installer built from `installer/` - is a combined work and must be distributed
    as a whole under the terms of GPL-3.0-or-later, including the corresponding-source
    obligations for the GPL-licensed parts.

Status of the Kokoro replacement (issue #21)
-------------------------------------------
Nox now ships a second text-to-speech engine, Kokoro (`voice.tts.engine: kokoro`, optional
extra `voice-kokoro`). Kokoro's own code, the ONNX Runtime it uses and the `kokoro-v1.0`
model files are permissively licensed. The `kokoro-onnx` package, however, still declares a
hard dependency on `phonemizer` (GPL-3.0-or-later) and `espeakng-loader` (which ships
espeak-ng, GPL-3.0-or-later) for its grapheme-to-phoneme front end.

Selecting Kokoro therefore removes Piper, but it does NOT by itself make a bundled build
Apache-2.0-only: the GPL obligations above apply unchanged to any distribution that includes
the `voice-kokoro` extra. Reaching an Apache-2.0-only installer needs a permissively licensed
grapheme-to-phoneme front end (or a Kokoro front end that accepts phonemes produced by one).
This is an open decision for the project, not a solved problem.
