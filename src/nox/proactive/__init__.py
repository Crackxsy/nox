"""Personality and proactivity runtime: when Nox speaks up on its own, and when it stays quiet.

Wired into the core by `nox.proactive.install.install(core)`, which returns the runtime the core
holds and stops again.
"""

from __future__ import annotations
